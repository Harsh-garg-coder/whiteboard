import styles from "./DrawingBoard.module.css";

export default function DrawingBoard() {
    return (
        <div className = {styles["drawingBoard-container"]}>
            drawingBoard
        </div>
    );
}